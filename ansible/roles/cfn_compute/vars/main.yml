---
stack_name: 'ansible-cfn-compute'

network_stack_name: 'ansible-cfn-network'

file_path: '{{ role_path }}/../../../cfn/compute.template'

key_name: "{{  lookup('env','KEY_NAME') }}"

ssh_location: "{{ lookup('env','SSH_LOCATION') }}"
